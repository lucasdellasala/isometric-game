/target
/build

# Development assets (Blender source files, 3D models, WIP renders)
/assets_dev/

# Sprite backups (created by asset generation agents)
assets/sprites/_backup/

# Binary source files (should never be in git)
*.blend
*.blend1
*.fbx

# Build artifacts
*.rar
*.gif

# Debug settings and logs (exported on game close, local to each developer)
debug_settings.json
boot_timing.log
boot_assets.log
*.log
